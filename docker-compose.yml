services:
  db:
    image: postgis/postgis:16-3.4
    container_name: adsb_db
    restart: always
    env_file:
      - envs/.local.env
    ports:
      # HOST PORT, CONTAINER PORT
      - 5432:5432
    volumes:
      # changed to a named volume and dev dir
      - ./pgdata:/var/lib/postgresql/data