{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Flight Parser (With Analysis)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "cwd": "${workspaceFolder}",
            "args": [
                "-analyze=true",
                "-out=flight_track.geojson"
            ]
        },
        {
            "name": "Launch Flight Parser (Skip Analysis)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "cwd": "${workspaceFolder}",
            "args": [
                "-analyze=false",
                "-out=flight_track_no_analysis.geojson"
            ]
        }
    ]
}